<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MedicineClient.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MedicineClient"
    Shell.FlyoutBehavior="Disabled"
    Title="MedicineClient"
    xmlns:Views="clr-namespace:MedicineClient.Views">
    <ShellContent Route="LoginPage" ContentTemplate="{DataTemplate Views:LoginPage}"/>
    <!--<ShellContent Route="AdminPage" ContentTemplate="{DataTemplate Views:AdminPage}"/>-->
    <!--<ShellContent Route="PharmacyPage" ContentTemplate="{DataTemplate Views:PharmacyPage}"/>-->
    <TabBar>
        <ShellContent
            Title="Logout"
            Icon="logout_icon.png"
            ContentTemplate="{DataTemplate Views:LogOut}" />
    </TabBar>
    
</Shell>
